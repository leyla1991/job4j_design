create table man(
	id serial primary key,
	name varchar(255),
	age int,
	weight float
);

insert into man (name, age, weight) values ('Leyla', (30), (65.2));
select * from man;

update man set name = 'Leila';
select * from man;

delete from man;
select * from man;
